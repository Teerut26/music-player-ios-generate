import React from "react";

interface Props {}

const Footer: React.FC<Props> = ({ }) => {
    return (
        <div className="text-center">
            แนะนำให้ใช้งานบน Application Google Chrome หรือ Google Chrome
            Desktop
        </div>
    );
};

export default Footer;
